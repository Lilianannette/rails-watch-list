<h1>Movie's List</h1>

<ul>
  <% @lists.each do |list| %>
  <li><%= link_to list.name, list_path(list)%> N°<%= list.id %></li>
  <% end %>
</ul>

<p><%= link_to "Add a new list", new_list_path %></p>
